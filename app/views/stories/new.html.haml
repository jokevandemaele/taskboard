#form
  #form-logo= image_tag "logo-projects.png"
  %h1.organization Create Story
  - remote_form_for @story, :url => project_stories_path(@project, :format => :json), :success => 'Stories.afterCreate(request.responseText);', :failure => 'ModalDialog.displayFormErrors(request.responseJSON);' do |f|
    = render :partial => 'form', :object => f
    = f.submit