%h1 Team Info
%hr
%h2 Users
- if @team.users.empty?
  %p This team has no members
- else
  %ul
    - @team.users.each do |user|
      %li= user.name
%hr
%h2 Projects
- if @team.projects.empty?
  %p This team has no projects
- else
  %ul
    - @team.projects.each do |project|
      %li= project.name