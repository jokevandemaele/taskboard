= form.error_messages 
.field
  = form.label :name
  = form.text_field :name

.field
  = form.label :color
  = form.text_field :color, :readonly => 'readonly'

:javascript
  $('team_color').setStyle({color: '##{@team.color}', background: '##{@team.color}'});
  $('team_color').observe('mousedown', function(event) {
    event.stop();
  });
  ColorPicker.attach('modal_dialog', '20px', '110px', 'team_color');

