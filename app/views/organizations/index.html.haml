.admin_list
  - cache do
    = render @organizations

- content_for :sidebar do
  #admin-sidebar
    %ul#admin-sidebar-menu
      - if current_user.admin?
        %li.new_organization New Organization
        %li.invite= link_to "send invitation", invite_path

- content_for :helpers do
  = javascript_include_tag 'helpers/organizations.js', 'helpers/modal_dialog.js'
  
