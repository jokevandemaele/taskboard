= render :partial => "menu" , :locals => { :team => @team, :view => @view }
= render :partial => "adder"
= render :partial => "taskboard_table", :locals => { :name => @team.name }
/ %script= "new YAHOO.util.DDTarget('taskboard');"
%script= "new YAHOO.util.DDTarget('taskboard-first-row');"

#dummy-for-actions

- content_for :helpers do
  = javascript_include_tag 'helpers/tasks.js', 'helpers/modal_dialog.js'
